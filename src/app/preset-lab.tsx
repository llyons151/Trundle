import { PresetLab } from '@/features/preset-lab/preset-lab';

/** Dev tool: compare time-picker preset styles at /preset-lab. */
export default function PresetLabScreen() {
  return <PresetLab />;
}
